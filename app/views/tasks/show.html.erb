<h1>タスク詳細画面</h1>
<p>名前: <%= @task.user.name %></p>
<p>タイトル: <%= @task.title %></p>
<p>内容: <%= @task.content %></p>
<p>終了期限: <%= @task.limit %></p>
<p>ステータス: <%= @task.status %></p>
<p>優先度: <%= @task.priority %></p>
<p>ラベル: 
  <% @task.labels.each do |label| %>
    <%= label.name %>
  <% end %>
</p>

<button type="button" class="btn btn-outline-primary btn-sm">
  <%= link_to "タスク一覧画面にもどる", tasks_path %>
</button>